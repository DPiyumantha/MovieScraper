
security:
  oauth2:
    resource:
      token-info-uri: http://127.0.0.1:9191/oauth/check_token
    client:
      client-id: MovieScraper
      client-secret: pin
spring:
  cloud:
    discovery:
      enabled: true

