
security:
  oauth2:
    resource:
      token-info-uri: http://localhost:9191/oauth/check_token
    client:
      client-id: MovieScraper
      client-secret: pin
spring:
  cloud:
    discovery:
      enabled: true

